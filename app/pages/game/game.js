import React from 'react';

const Game = ({id}) => <div>
  <h1>You joined game {id}</h1>
</div>;

/*
const options = {
  propTypes: {
    id: React.PropTypes.string.isRequired
  }
};
*/

export default Game;
